<?xml version="1.0" encoding="utf-8" ?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="tickets.CustomCells.TextInViewCell">

    <Grid ColumnSpacing="2" Padding="5">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        
        <Frame Grid.Row="0" Grid.Column="1" BackgroundColor="#0535f0" CornerRadius="15" HasShadow="True" >
            <StackLayout>
                <Label TextColor="White" Text="{Binding Text}" />
            </StackLayout>
        </Frame>
        <Label FontSize="Micro" Grid.Row="1" Grid.Column="1" Text="{Binding Date, StringFormat='{0:MM/dd/yyyy hh:mm tt}'}" TextColor="Gray"></Label>
    </Grid>
</ViewCell>